@model IEnumerable<ShopQuanAoLite.Models.BoSuuTap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<ul class="breadcrumb">
    <li>
        <i class="icon-home"></i>
        <a href="/Admin/Home">Trang chủ</a>
        <i class="icon-angle-right"></i>
    </li>
    <li>
        Bộ Sưu Tập
        <i class="icon-angle-right"></i>
    </li>
    <li><a href="#">Danh mục sản phẩm</a></li>
</ul>

<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-header" data-original-title>
            <h2>Danh mục sản phẩm</h2>
            <div class="box-icon">
                <a href="#" class="btn-minimize"><i class="halflings-icon white chevron-up"></i></a>
                <a href="#" class="btn-close"><i class="halflings-icon white remove"></i></a>
            </div>
        </div>

        <div class="box-content">

            <table class="table table-striped table-bordered bootstrap-datatable datatable">
                <thead>
                    <tr>
                        <th>
                            Mã Loại
                        </th>
                        <th>
                            Tên danh mục
                        </th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr id="row_@item.MaBST">
                            <td>
                                @item.MaBST
                            </td>
                            <td>
                                @item.TenBST 
                            </td>
                            <td>
                                <a class="btn btn-info" href="/Admin/BoSuuTap/Edit/@item.MaBST"><i class="halflings-icon white edit"></i> Sửa</a>
                                <a class="btn btn-danger" data-ajax="true" data-ajax-complete="$('#row_@item.MaBST').remove()" data-ajax-confirm="Bạn có muốn xóa bản ghi này không?" data-ajax-method="Delete" href="/Admin/BoSuuTap/Delete/@item.MaBST">
                                    <i class="halflings-icon white trash"></i> Xóa
                                </a>
                            </td>
                        </tr>
                    }                   
                </tbody>
            </table>
            <p>Lưu ý: Phải xóa toàn bộ sản phẩm tồn tại trong bộ sưu tập mới có thể tiến hành xóa.</p>
            <a class="btn btn-success" href="@Url.Action("Create","BoSuuTap")">
                <i class="halflings-icon white zoom-in"></i>Thêm mới
            </a>
        </div>
    </div><!--/span-->
</div><!--/row-->